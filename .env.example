# Application
APP_ENV=development
APP_TIMEZONE=Europe/Istanbul

# Database
POSTGRES_DB=portfoy_os
POSTGRES_USER=portfoy
POSTGRES_PASSWORD=change-me-locally
DATABASE_URL=postgresql+psycopg://portfoy:change-me-locally@localhost:5432/portfoy_os

# Redis
REDIS_URL=redis://localhost:6379/0

# AI analyst — not used in MVP bootstrap
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=deepseek-v4-pro

# Financial data — add only when adapters are implemented
TCMB_EVDS_API_KEY=
